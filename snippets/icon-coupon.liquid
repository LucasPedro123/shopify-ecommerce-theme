{%- doc -%}
  icon-coupon — Ícone de etiqueta de cupom (price tag style).

  Renderizado em currentColor para herdar a cor do contexto (ex: dentro do ui-chip
  variant 'cupom', herda a cor brand-800).

  @example
  {%- render 'ui-chip', label: 'Cupom: LUXO15', variant: 'cupom', icon: 'icon-coupon' -%}
{%- enddoc -%}

<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false">
  <path d="M20.59 13.41l-7.17 7.17a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z"/>
  <line x1="7" y1="7" x2="7.01" y2="7"/>
</svg>
